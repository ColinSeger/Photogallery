---
title: "Photogallery"
layout: "base.njk"
---

<section style="width: 100%;">
    <h1>Photogallery</h1>
    <br/>
    <br/>
    <p>Here are my atempts att geting images to display properly.</p>
    <ul>
        {% asyncEach image in images %}
        <div class="img-container">
            <h1 class="image-title">{{ image.title }}</h1>
            <br/>
            <img src="{{ image.image }}" alt="{{ image.imageAlt }}">
            <div class="img-click">
                <h1>{{ image.title }}</h1>
                <br/>
                <br/>
                <img src="{{ image.image }}" alt="{{ image.imageAlt }}">
                <p>
                    {{ image.description }}
                </p>
            </div>
        </div>
        
        {% endeach %}
    </ul>
</section>