---
title: "Photogallery"
layout: "base.njk"
---

<section>
    <h1>Photogallery</h1>
    <br/>
    <br/>
    <p>Here are some photos made by ai.</p>
    <ul>
        {% for photo in collections.photos %}
            <img src="{{ photo.data.image }}" alt="{{ photo.data.imageAlt }}">
            {{ image }}
        {% endfor %}
    </ul>
</section>