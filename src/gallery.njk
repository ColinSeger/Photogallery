---
title: "Photogallery"
layout: "base.njk"
---

<section style="width: 100%;">
    <h1>Photogallery</h1>
    <p>Here are my atempts at getting images to display properly.</p>
    <ul>
        <div class="background-shade"></div>
        {% asyncEach image in images %}
        <div class="img-container">
            <div class="image-head">
                <h1 class="image-title">{{ image.title }}</h1>
            </div>
            
            <div class="display-image" style="background-image: url({{ image.image }});">

            </div>
            <div class="image-footer">

            </div>
            {# <img src="{{ image.image }}" alt="{{ image.imageAlt }}"> #}
            <div class="img-click">
                <h1>{{ image.title }}</h1>
                <img src="{{ image.image }}" alt="{{ image.imageAlt }}">
                <p>
                    {{ image.description }}
                </p>
            </div>
        </div>
        
        {% endeach %}
    </ul>
</section>